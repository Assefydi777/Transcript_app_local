# Optional heavyweight audio features. Install only when you need VAD via Silero
# or pyannote diarization, preferably on a GPU/Linux machine.
torch==2.5.1
torchaudio==2.5.1
silero-vad==5.1.2
pyannote.audio==3.3.2
